---
# One-time system setup requiring sudo.
# Installs tools, k3s (Linux), dnsmasq, and configures DNS resolution.
# Run once with: make setup
- name: System setup (requires sudo)
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  roles:
    - prerequisites
    - k3s
    - dnsmasq
